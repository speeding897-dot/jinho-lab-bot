name: Jinho Lab Daily Job

on:
  schedule:
    - cron: '0 22 * * *'  # 매일 한국시간 오전 7시
  workflow_dispatch:      # 수동 실행 버튼

# [추가] 깃허브 로봇에게 파일 쓰기 권한을 부여합니다.
permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 저장소 체크아웃
        uses: actions/checkout@v3

      - name: 파이썬 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: 라이브러리 설치
        run: |
          pip install requests beautifulsoup4

      - name: 1. 인크루트 사기업 데이터 수집 (collector.py)
        run: python collector.py

      - name: 2. 공기업 채용공고 갱신 (main.py)
        run: python main.py

      - name: 3. 사기업 채용공고 갱신 (main_private.py)
        run: python main_private.py

      - name: 결과 깃허브에 반영 (Commit & Push)
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add .
          # 변경된 모든 파일(html, json, txt)을 강제로 포함시킵니다.
          git commit -m "🚀 공기업/사기업 채용공고 자동 업데이트 [$(date)]" || echo "변경사항 없음"
          git push
